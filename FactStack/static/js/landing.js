import createTopic from "./utils/"
import fetchProfileInfo from './utils/'
// todo change './utils/...' == to => 
const topicPage = document.getElementById("topicTable")

console.log(topicPage)


window.onload(() => {
  // fetch topic data here
  // fetch quiz data here  *** as a promise so these run in parallel

  // create topicsTable
  // create quizHistoryTable
})